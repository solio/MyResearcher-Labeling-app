# MyResearcher-Labeling-app 使用说明与接口说明

版本：v1.2（2026-09-08） · 适用代码：`main` 分支 `7ac08b8` 及之后
关联文档：`README.md`（快速上手/真机清单）、`HANDOFF.md`（数据契约细节/集成开放问题）

单人手机标注工具：默认后端 Python 标准库（`http.server` + `sqlite3`，零 pip），可选 MySQL 存储
（需 `pip install pymysql`）；前端原生 JS PWA（零构建零 CDN）。标签体系抄录自
`MyResearcher-ModelTraining/schema/semantic-schema-calibrated-v0.2.1.json`（只读）；中文释义在
`schema/annotation-schema.v1.json`，**owner 可直接编辑，重启服务生效**。

v0.3 的五个稀疏辅助因子使用独立的 `semantic-schema-candidate-v0.3` 批次；导入时显式指定
`--schema-version semantic-schema-candidate-v0.3`，旧 v0.2.1 七头批次用法不变。

---

## 第一部分 使用说明

### 1. 快速开始（60 秒）

```bash
cd MyResearcher-Labeling-app
python3 tools/seed_demo.py          # 可选：20 条虚构文本 demo batch
python3 server.py --port 8787       # 启动，自动打印 局域网 URL
```

控制台输出示例：

```
MyResearcher Labeler 已启动  schema=semantic-schema-calibrated-v0.2.1  storage=sqlite
  本机:   http://127.0.0.1:8787
  局域网: http://192.168.x.x:8787   （手机连同一 Wi-Fi 打开此地址）
  后端:   /…/data/labeler.db
  审计:   /…/data/annotations.jsonl
```

浏览器（或手机浏览器）打开 URL → 选 Batch → 选 Head（本次会话锁定）→ 进入标注。

> 本机排障：若机器 shell 配有 `all_proxy` 等代理变量且代理不可用，`curl http://127.0.0.1:8787`
> 会被劫持而失败，请加 `--noproxy '*'`；手机直连浏览器不受影响。

#### 1.1 存储模式与配置文件（sqlite / mysql）

存储后端由配置文件决定，默认读项目根 `config.json`，所有入口支持 `--config 指定路径`：
`server.py`、`tools/gpt_tasks.py`（新增）、`tools/import_batch.py`、`tools/export_annotations.py`（后两者仍保留 `--db` 直连 sqlite 的旧用法）。

```json
{
  "storage": "sqlite",
  "jsonl_path": "data/annotations.jsonl",
  "sqlite": { "db_path": "data/labeler.db" },
  "mysql": { "host": "127.0.0.1", "port": 3306, "user": "labeler",
             "password": "***", "database": "myresearcher_labeler",
             "charset": "utf8mb4", "connect_timeout": 10, "ssl_ca": null }
}
```

- **无 `config.json` 时默认 sqlite**，历史用法完全不变。
- 配置含数据库凭据，**已 gitignore**；模板 `config.example.json`，复制改名。
- 相对路径按**配置文件所在目录**解析：服务端 `config.json`（`mysql.host`=内网 IP）与本机
  `data/config-gpt.json`（`mysql.host`=公网 IP）是仅 host 不同的两份配置，指向同一个
  MySQL 库——这正是"本机 GPT 加任务 → 服务端网页标注 → 本机拉结果"的迁移方式，见 1.2。
- 校验 fail-closed：未知字段、缺 `mysql.host/user/password/database` → 报错退出码 **2**，不静默降级。
- `storage=mysql` 需要 `pip install pymysql`（纯 Python 驱动，唯一非标准库依赖；sqlite 模式仍零安装）。
- MySQL 端尺寸约束：`batch_id ≤64 字符`、`sample_id ≤255 字符`；超限导入整批回滚（fail-closed）。
- 两种模式都会写 jsonl 审计流水（配置 `jsonl_path`），行为一致。

#### 1.2 部署与 GPT 闭环（当前实际形态，2026-09-08 已部分实测）

- **服务端**：docker compose 起唯一服务 labeler（预构建 amd64 私有镜像
  `…cr.aliyuncs.com/fangzuzu/labeler:v1`，服务器不 build、不依赖 Docker Hub），只绑
  `127.0.0.1:8787` 交给已有 nginx 反代。MySQL 用服务端**已有实例**的 `fzz-config` 库与
  同名账号（labeler 首次启动自动建 4 张表），服务端 `config.json` 的 `mysql.host` 必须
  填内网 `172.21.153.219`。首次部署照抄命令序列见 README「部署到服务端」。
- **本机（GPT 侧）**：`data/config-gpt.json` 已配置（`mysql.host`=公网 `8.148.251.114:3306`，
  安全组已放行），经公网直连同一库；`tools/gpt_tasks.py add/pull` 已实测双向通。
- **闭环**：本机 `add` 投任务 → 手机/网页标注 → 本机 `pull --final-only` 收结果。
  库内现有链路自检批次 `selftest-gpt-link`（2 样本 × 2 head），可用于服务端起来后的
  连通验证：`curl http://127.0.0.1:8787/api/batches` 应能看到它。
- 未实测项：服务端容器 → 内网 MySQL 的连通（以服务器上 curl 为准）、真实 nginx 挂载。

### 2. 导入真实数据

`samples.jsonl`，每行一个 JSON 对象：

```json
{"sample_id":"a-001","text":"帖子正文（必填）","title":"可选标题",
 "metadata":{"date":"2026-08-01","stock_code":"DM0001","stock_name":"虚构甲",
             "source":"股吧","split_provenance":"dev/batch_a_v1"}}
```

```bash
python3 tools/import_batch.py \
  --batch batch_a \
  --file samples.jsonl \
  --heads target_mode,stance,emotion_primary,emotion_target,action_tendency,context_dependency,reasoning_tags
```

规则：
- 必填 `sample_id`、`text`（非空字符串）；可选 `title`、`metadata`（对象，原样落库）。
- **fail-closed**：缺必填 / 文件内或与库内重复 `sample_id` / 未知 head / 未知顶层字段
  → 报错退出码 **1**，完全不写库。
- 每个 sample × 每个 head 生成一条任务，`position` 按文件顺序从 0 递增；向已有 batch 追加导入时续接。

### 3. 手机访问与添加主屏幕

1. 电脑与手机连**同一 Wi-Fi**，手机浏览器打开局域网 URL。
2. 添加到主屏幕（PWA，图标为占位图）：
   - iOS Safari：分享 → 添加到主屏幕；
   - Android Chrome：⋮ → 添加到主屏幕 / 安装应用。
3. 首次打开会自动恢复上次会话（batch/head/进度/已填答案）。
4. 选择页会显示选中 batch 下**各 head 的剩余量（剩 N / M）**，标注后 ⇄ 返回选择页时
   自动刷新，方便决定接下来填哪个 head。

### 4. 界面与标注流程

四段固定布局（适配 360×800，无横向滚动）：

```
┌──────────────────────────────┐
│ ① 顶栏：进度 x/N·完成y | head名 | ? | ⇄换head | 更多信息 │
│ ② 内容卡片（长文在卡内滚动）                            │
│ ③ head 问题句 + ?                                     │
│ ④ 标签区（独立滚动，按钮≥44px）+ disposition 行        │
│ 底栏：上一条 | 完成本条 | 下一条                        │
└──────────────────────────────┘
```

- **Head Lock**：一个会话只锁一个 head；`⇄ 换head` → 确认 → 返回选择页，进度保留。
- **批次完成页**：标完当前 head 最后一条，或进入一个已全部完成的 batch 时，若该批次
  所有任务均为 final/终态处置，自动弹出"本批次已全部填完"（每批次每次会话只弹一次，
  可关闭可点"返回选择页"）；选择页该批次行显示"已完成 ✓ N / N"。
- **归档**：批次行右侧"归档"按钮 → 二次确认 → 批次从列表隐藏、不可再标注，数据保留
  并导出 CSV 到服务器 `data/exports/`（详见 9.6）。页面上不可撤销。
- **单选 head**（target_mode / stance / emotion_primary / emotion_target / action_tendency /
  context_dependency）：点标签即存草稿，再点"完成本条"置 final 并跳下一条未完成。
- **多选 head**（reasoning_tags，15 标签）：每次 toggle 即存草稿；可多选后"完成本条"。
- **释义**：head 旁 `?` / 问题句旁 `?` → head 释义 sheet（定义、该选/不该选、正例、易混淆）；
  标签旁 `ⓘ` → 标签释义 + 全局不变量。均为 bottom sheet，不跳页、不丢已选。
- **更多信息**：date / stock_code / stock_name / source / split_provenance / sample_id / title。
- **disposition**（每条均可标）：`无法判断`、`缺少上下文`、`跳过`（三者=终态，标记后跳下一条，
  不再进入 resume）、`稍后再看`（仍算未完成，留在本条）。**再点一次已选中的 chip = 取消**
  （清空处置、保留已选答案；误触终态被跳走后，用"上一条"返回同样可取消，该条重新变为未完成）。
- **禁止预填**：界面与接口均不展示任何模型预测。

### 5. 保存链路（断网不丢）

```
点击标签/完成/处置
  → ① 先写 localStorage 重试队列（同一条目只保留最新快照）
  → ② POST /api/annotations（幂等 upsert，revision+1）
       成功 → toast「已入库」
       失败 → toast + 底部横幅「本机已保存 · 待同步 N 条」
  → ③ 网络恢复自动重试（online 事件 / 5s 失败重试 / 10s 定时 / 启动时）
```

刷新、关页、断网后重开：经 `/api/resume` + localStorage（会话、队列、列表与详情缓存）恢复到
原 batch/sample/head 与已填答案；**已点击的答案任何情况不丢**。
注意：服务进程不可达时**整页冷启动**无法加载（无 Service Worker，属文件结构约束）；页面保持
打开则完全可用。

### 6. 导出

```bash
python3 tools/export_annotations.py --format jsonl > out.jsonl          # 全量（含未标注行）
python3 tools/export_annotations.py --format jsonl --final-only > gold.jsonl
python3 tools/export_annotations.py --format csv > out.csv
```

### 7. 重置 / 测试

```bash
rm -rf data/ && python3 tools/seed_demo.py   # 重置为干净 demo（仅 sqlite 模式；mysql 模式的数据在库里，勿对生产库执行）
python3 -m unittest discover tests           # 37 个用例，应全绿（MySQL 集成 3 个为 opt-in，未设环境变量时整体跳过）
```

MySQL 集成测试需真实库，设置环境变量后运行（否则自动跳过）：

```bash
MR_LABELER_TEST_MYSQL='{"host":"127.0.0.1","port":13306,"user":"labeler",
  "password":"…","database":"myresearcher_labeler"}' \
  python3 -m unittest tests.test_config_and_tools.TestMysqlStore
```

### 8. 常见问题

| 现象 | 原因与处理 |
|---|---|
| 手机打不开页面 | 电脑防火墙拦截 8787 / 不在同一网段 / 用了错误的 IP（以启动打印为准） |
| curl 本机 API 无响应 | shell 代理变量劫持，加 `--noproxy '*'` |
| 标签点击后横幅"待同步"不消失 | 服务未恢复；恢复后 5–10s 自动同步，无需操作 |
| 想改问题句/释义 | 编辑 `schema/annotation-schema.v1.json` 后重启 server.py；**不得改 label id 与顺序** |
| 换了标签体系版本 | 新建 batch 导入；旧 batch 的 assignment 词表不自动迁移 |
| 启动报 `storage=mysql 需要 MySQL 驱动` | 运行 server.py 的机器执行 `pip install pymysql` |
| 启动报配置错误（未知字段/缺字段） | 对照 `config.example.json` 检查 `config.json`；配置错误退出码为 2 |
| mysql 模式导入报超限/回滚 | `batch_id` 超 64 字符或 `sample_id` 超 255 字符（MySQL 键长约束），改用更短 id |

---

## 第二部分 接口说明

### 9. HTTP API

Base URL：`http://<host>:8787`。除静态文件外均为 JSON；错误统一 `{"error": "..."}` +
对应 4xx/5xx；响应头 `Cache-Control: no-store`。

#### 9.1 GET /api/resume — 最近未完成任务

```bash
curl --noproxy '*' http://127.0.0.1:8787/api/resume
```

```json
{"batch_id": "demo", "assignment_id": "demo:demo-003:reasoning_tags"}
```

- "未完成" = 无标注记录，或 `is_final=0` 且 disposition ∉ {跳过, 无法判断, 缺少上下文}。
- 排序：最近有活动（updated_at 降）优先；从未触碰的按 position 升序。
- 全部完成 → `{"batch_id": null, "assignment_id": null}`。

#### 9.2 GET /api/batches — batch 列表

```json
[{"id": "demo", "name": "demo", "created_at": "2026-09-07T09:03:29Z",
  "schema_version": "semantic-schema-calibrated-v0.2.1", "total": 140, "done": 3}]
```

`done` = `is_final=1` 或终态 disposition 的任务数。

#### 9.3 GET /api/assignments?batch_id=&head= — 任务清单

两个参数必填，head 必须在 7 个 head 内（否则 400）。

```json
[{"id": "demo:demo-001:stance", "batch_id": "demo", "sample_id": "demo-001",
  "head": "stance", "position": 0, "status": "in_progress", "title": "[虚构demo] …",
  "answer": "BULL", "disposition": null, "is_final": false,
  "revision": 1, "updated_at": "2026-09-07T09:06:23Z"}]
```

按 `position` 升序；未标注行 `answer/disposition/updated_at/revision` 为 `null`，`is_final=false`。

#### 9.4 GET /api/assignment?id= — 单条详情（含释义）

```json
{
  "assignment": {"id": "demo:demo-001:stance", "batch_id": "demo",
                  "sample_id": "demo-001", "head": "stance", "position": 0, "status": "in_progress"},
  "sample": {"id": "demo-001", "title": "…", "content": "…",
              "metadata": {"date": "…", "stock_code": "…", "split_provenance": "demo/seed_v1", …}},
  "answer": null, "disposition": null, "is_final": false, "revision": 0, "updated_at": null,
  "schema_version": "semantic-schema-calibrated-v0.2.1",
  "glossary": {"type": "single", "question": "作者对未来方向的预期是？",
                "definition_zh": "…", "select_when": "…", "not_select_when": "…",
                "positive_examples": ["…"], "confusable": ["…"],
                "labels": [{"id": "BULL", "name_zh": "看多", "description": "…"}, …]},
  "invariants": ["UNKNOWN≠NEUTRAL：…", …],
  "dispositions": ["无法判断", "缺少上下文", "跳过", "稍后再看"]
}
```

`id` 不存在 → 404。

#### 9.5 POST /api/annotations — 保存（幂等 upsert）

```bash
curl --noproxy '*' -X POST http://127.0.0.1:8787/api/annotations \
  -H 'Content-Type: application/json' \
  -d '{"assignment_id":"demo:demo-001:stance","answer":"BULL","disposition":null,"is_final":false}'
```

请求体：

| 字段 | 类型 | 说明 |
|---|---|---|
| `assignment_id` | string | 必填，`{batch_id}:{sample_id}:{head}` |
| `answer` | string \| string[] \| null | 单选 head 传标签字符串；`reasoning_tags` 传字符串数组（自动去重保序）；可为 null（仅标 disposition 时） |
| `disposition` | string \| null | 中文枚举：无法判断 / 缺少上下文 / 跳过 / 稍后再看 |
| `is_final` | bool | true 表示定稿 |

响应（200）：

```json
{"ok": true, "assignment_id": "demo:demo-001:stance", "batch_id": "demo", "sample_id": "demo-001",
 "head": "stance", "answer": "BULL", "disposition": null, "is_final": false,
 "revision": 2, "updated_at": "2026-09-07T09:06:23Z", "schema_version": "semantic-schema-calibrated-v0.2.1"}
```

语义：
- 按 `assignment_id` 幂等 upsert：重复提交不产生新行，`revision` 每次 +1，`updated_at` 刷新；
- 语义校验：标签不在该 head 词表 → **400**；数组类型用错 → 400；assignment 不存在 → **404**；
  assignment 所属批次已归档 → **400**（"批次已归档，禁止标注"）；
- **清除标注**：`answer=null, disposition=null, is_final=false` 合法（用于取消误触的
  disposition），revision+1、状态回未完成；但 `is_final=true` 时空标注 → **400**；
- 每次保存同时 append 一行到 `data/annotations.jsonl`（完整流水，含 revision）；
- 副作用：`is_final=1` 或终态 disposition → `assignments.status='completed'`，否则 `in_progress`。

#### 9.6 POST /api/batch/archive — 归档批次

```bash
curl --noproxy '*' -X POST http://127.0.0.1:8787/api/batch/archive \
  -H 'Content-Type: application/json' -d '{"batch_id":"mybatch"}'
```

响应（200）：

```json
{"ok": true, "batch_id": "mybatch", "archived_at": "2026-09-09T08:00:00Z",
 "export_path": "/…/data/exports/mybatch.csv", "export_rows": 350}
```

语义：
- 给 `batches.archived_at` 打时间戳，**数据全部保留不动**；
- 同步导出全量 9 列 CSV（含未标注行，utf-8-sig 便于 Excel）到
  `data/exports/<batch_id>.csv`（非法文件名字符替换为 `_`）；
- 归档后：`GET /api/batches` 不再返回该批次、`GET /api/resume` 跳过其任务、
  `GET /api/assignments` → **404**（"批次已归档"）、写入 → **400**；
- 重复归档 → **400**；批次不存在 → **404**。页面上不可撤销（如需恢复需直接改库）。

#### 9.7 静态资源

其余路径返回 `static/` 下文件（`/` → `index.html`）。已注册类型：html/js/css/json/webmanifest/
png/svg/ico/txt；路径穿越返回 403；未命中返回 404（JSON）。

### 10. CLI 接口

#### server.py

```
python3 server.py [--config config.json] [--port 8787] [--host 0.0.0.0] [--db data/labeler.db] [--jsonl data/annotations.jsonl]
```

- `--config` 指定配置文件（默认项目根 `config.json`，不存在则 sqlite 默认值）。
- `--db`/`--jsonl` 仅 sqlite 模式有效；mysql 模式下传入会报错退出码 **2**。
- 启动时建表（IF NOT EXISTS；sqlite 为 WAL，mysql 为 utf8mb4 InnoDB），加载
  `schema/annotation-schema.v1.json`，打印本机/局域网 URL 与后端信息。
- 配置错误（含缺 pymysql 驱动）退出码 **2**。

#### tools/gpt_tasks.py（GPT 专用：加任务 / 拉结果）

```
python3 tools/gpt_tasks.py add --batch NAME --file samples.jsonl --heads h1,h2[,…] [--config config.json]
python3 tools/gpt_tasks.py add --batch NAME --assignment-file assignments.jsonl \
  --schema-version semantic-schema-candidate-v0.3 [--config config.json]
python3 tools/gpt_tasks.py pull [--format jsonl|csv] [--final-only] [--batch NAME] [--out FILE] [--config config.json]
python3 tools/gpt_tasks.py status --batch NAME [--config config.json]
```

- 与服务端读同一份配置文件，sqlite/mysql 通用；`add` 与 `import_batch` 同一套 samples.jsonl 契约。
- `--assignment-file` 为逐行 sparse 任务格式（每行含 `sample_id`、`text`、非空 `heads` 数组）；
  v0.3 辅助因子必须同时指定 `--schema-version semantic-schema-candidate-v0.3`。
- `add` 退出码：0 成功；1 fail-closed（不写库）；2 配置错误。
  成功输出：`[gpt_tasks] OK add batch=… samples=N heads=M assignments=N×M storage=…`
- `pull` 行格式与导出 JSONL/CSV 完全一致（见 11.3）；默认打印 stdout，`--out` 写文件；统计打印 stderr。
- `status` 输出一行 JSON：`{"batch","total","done","finals","complete"}`（done = final 或终态
  处置，与页面 done 口径一致）；批次不存在或无任务 → 退出码 **1**。

#### tools/import_batch.py

```
python3 tools/import_batch.py --batch NAME --file samples.jsonl --heads h1,h2[,…] \
  [--schema-version semantic-schema-calibrated-v0.2.1] [--db data/labeler.db]
```

- 退出码：0 成功；1 fail-closed（缺必填/重复 sample_id/未知 head/未知字段/空文件/文件不存在）。
- 成功输出：`[import_batch] OK batch=… samples=N heads=M assignments=N×M db=…`

#### tools/export_annotations.py

```
python3 tools/export_annotations.py [--format jsonl|csv] [--final-only] [--db data/labeler.db]  > 输出文件
```

- 结果打印到 **stdout**；统计打印到 stderr；库不存在退出码 1。
- 行序：batch → head → position。

#### tools/seed_demo.py

```
python3 tools/seed_demo.py [--batch demo] [--db data/labeler.db] [--heads 全部7个(缺省)]
```

20 条**虚构**文本（股票代码 DM0001–DM0005 均为虚构），`split_provenance=demo/seed_v1`。

### 11. 数据契约

#### 11.1 标识与存储

- `assignment_id = {batch_id}:{sample_id}:{head}`（全局主键）。
- SQLite 表（`data/labeler.db`，运行时创建）：

```sql
batches(id TEXT PK, name TEXT, created_at TEXT, schema_version TEXT)
samples(batch_id TEXT, id TEXT, title TEXT, content TEXT, metadata_json TEXT, PK(batch_id,id))
assignments(id TEXT PK, batch_id TEXT, sample_id TEXT, head TEXT, position INTEGER,
            status TEXT DEFAULT 'pending')          -- pending/in_progress/completed
annotations(assignment_id TEXT PK, answer_json TEXT, disposition TEXT,
            is_final INTEGER, revision INTEGER, updated_at TEXT)  -- updated_at: UTC ISO8601 Z
```

- MySQL 表（`storage=mysql` 时建在配置指定的库，utf8mb4/InnoDB，语义与 SQLite 完全一致）：

```sql
batches(id VARCHAR(64) PK, name VARCHAR(255), created_at VARCHAR(32), schema_version VARCHAR(64))
samples(batch_id VARCHAR(64), id VARCHAR(255), title TEXT, content MEDIUMTEXT,
        metadata_json MEDIUMTEXT, PK(batch_id,id))
assignments(id VARCHAR(512) PK, batch_id VARCHAR(64), sample_id VARCHAR(255), head VARCHAR(64),
            position INT, status VARCHAR(32) DEFAULT 'pending')
annotations(assignment_id VARCHAR(512) PK, answer_json MEDIUMTEXT, disposition VARCHAR(64),
            is_final TINYINT, revision INT, updated_at VARCHAR(32))
```

由此的尺寸约束：`batch_id ≤64`、`sample_id ≤255` 字符（`assignment_id ≤512`）；超限导入整批回滚。

#### 11.2 导入 samples.jsonl

| 字段 | 必填 | 落库 |
|---|---|---|
| `sample_id` | ✓ | samples.id |
| `text` | ✓ | samples.content |
| `title` |  | samples.title |
| `metadata` |  | samples.metadata_json，原样保留（sort_keys） |

#### 11.3 导出 JSONL（每行一个对象）

```json
{"batch": "demo", "sample_id": "demo-001", "head": "stance",
 "answer": "BULL",              // 字符串；reasoning_tags 为数组；未标注 null
 "disposition": null,           // 中文枚举或 null
 "is_final": false,
 "schema_version": "semantic-schema-calibrated-v0.2.1",
 "updated_at": "2026-09-07T09:06:23Z",   // 未标注 null
 "metadata": {"date": "…", "stock_code": "…", "stock_name": "…",
               "source": "…", "split_provenance": "…"}}
```

CSV 为同字段 9 列表头：`batch,sample_id,head,answer,disposition,is_final,schema_version,
updated_at,metadata`；数组/对象序列化为 JSON 字符串，`is_final` 用 1/0。默认含未标注行。

#### 11.4 data/annotations.jsonl（保存流水）

每次 POST append 一行：字段同导出 JSONL，另含 `assignment_id` 与 `revision`；
是"操作流水"，与 DB"最终态"互补，回放可重建 revision 历史。

### 12. 标签词表（7 head，顺序即 class_order，不可改）

| head | 类型 | 标签（按序） |
|---|---|---|
| target_mode | 单选 | ON_TARGET, CROSS_TARGET, MARKET_GENERAL, UNKNOWN |
| stance | 单选 | BULL, BEAR, NEUTRAL, MIXED, UNKNOWN |
| emotion_primary | 单选 | FEAR, ANXIETY, ANGER, FRUSTRATION, REGRET, HOPE, EXCITEMENT, FOMO, CALM, NONE_EXPLICIT, UNKNOWN |
| emotion_target | 单选 | PRICE, POSITION, COMPANY, MARKET, OTHER, NOT_APPLICABLE, UNKNOWN |
| action_tendency | 单选 | BUY, ADD, HOLD, DO_T, REDUCE, SELL, WATCH, NO_ACTION_SIGNAL, UNKNOWN |
| context_dependency | 单选 | SELF_CONTAINED, PARTIAL_CONTEXT, EXTERNAL_CONTEXT_REQUIRED, UNKNOWN |
| reasoning_tags | 多选(15) | FUNDAMENTAL, VALUATION, TECHNICAL_PRICE, FLOW_POSITIONING, NEWS_EVENT, RUMOR, SOCIAL_PROOF, MACRO_POLICY, THEME_NARRATIVE, NO_REASON_GIVEN, RELATIVE_PERFORMANCE, CROSS_STOCK_REFERENCE, SARCASM_IRONY, WORDPLAY, UNKNOWN |

标签释义（中文名/描述/易混淆）见 `schema/annotation-schema.v1.json`，或标注时点"?"与"ⓘ"。

### 13. 客户端（localStorage）约定

| Key | 内容 |
|---|---|
| `mr_labeler_session_v1` | `{batchId, head}`（Head Lock 会话） |
| `mr_queue_v1` | 重试队列 `[{assignment_id, queued_at, payload:{assignment_id, answer, disposition, is_final}}]`，同 assignment 只留最新 |
| `mr_details_v1` | 已访问详情缓存（≤200 条，LRU 淘汰） |
| `mr_list_v1__{batch}__{head}` | 任务列表缓存（离线恢复用） |
